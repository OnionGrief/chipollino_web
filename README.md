# Chipollino web
Окружение для разработки:
- `devenv shell`

Запуск сервера:
- `devenv up`

Для сборки Docker-образов:
- `buildContainers`

Запуск через compose с собранными образами:
- `docker compose up -d`
